# 🚀 Как запустить бота с GitHub

## Шаг 1: Клонирование проекта

### Вариант А: Через командную строку/Git Bash

```bash
# Перейдите в папку, где хотите разместить проект
cd C:\Users\alex_\OneDrive\Desktop

# Клонируйте репозиторий
git clone https://github.com/alexsychi-prog/GruzcoBot.git

# Перейдите в папку проекта
cd GruzcoBot
```

### Вариант Б: Через GitHub Desktop

1. Установите GitHub Desktop: https://desktop.github.com/
2. Войдите в свой аккаунт GitHub
3. Нажмите "File" → "Clone repository"
4. Выберите `alexsychi-prog/GruzcoBot`
5. Выберите папку для клонирования
6. Нажмите "Clone"

---

## Шаг 2: Создание виртуального окружения

```bash
# Создайте виртуальное окружение
python -m venv venv

# Активируйте виртуальное окружение
# Windows:
venv\Scripts\activate

# Linux/Mac:
source venv/bin/activate
```

После активации в начале строки появится `(venv)`.

---

## Шаг 3: Установка зависимостей

```bash
# Установите все необходимые пакеты
pip install -r requirements.txt
```

---

## Шаг 4: Настройка переменных окружения

### 4.1. Создайте файл `.env`

Создайте файл `.env` в корне проекта (рядом с `requirements.txt`).

### 4.2. Получите токен бота

1. Откройте Telegram
2. Найдите бота [@BotFather](https://t.me/BotFather)
3. Отправьте команду `/newbot`
4. Следуйте инструкциям и создайте бота
5. Скопируйте токен (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 4.3. Получите свой Telegram ID

1. Найдите бота [@userinfobot](https://t.me/userinfobot)
2. Отправьте команду `/start`
3. Скопируйте ваш ID (число, например `123456789`)

### 4.4. Заполните файл `.env`

Откройте файл `.env` и заполните:

```env
# Telegram Bot Configuration
BOT_TOKEN=ваш_токен_от_BotFather
ADMIN_TELEGRAM_ID=ваш_telegram_id

# Database URL (по умолчанию SQLite)
DATABASE_URL=sqlite+aiosqlite:///data/bot.db

# Log Level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
```

**Пример:**
```env
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
ADMIN_TELEGRAM_ID=123456789
DATABASE_URL=sqlite+aiosqlite:///data/bot.db
LOG_LEVEL=INFO
```

---

## Шаг 5: Запуск бота

### Вариант А: Через командную строку

```bash
# Убедитесь, что виртуальное окружение активировано
# (должно быть (venv) в начале строки)

# Запустите бота
python -m bot.main
```

### Вариант Б: Через скрипт (Windows)

Дважды кликните на файл `start_bot.bat`

### Вариант В: Через Python напрямую

```bash
python bot/main.py
```

---

## ✅ Проверка работы

Если бот запустился успешно, вы увидите в консоли:
```
Бот запущен и готов к работе!
```

Теперь:
1. Откройте Telegram
2. Найдите вашего бота по username
3. Отправьте команду `/start`
4. Бот должен ответить и показать меню

---

## 🔄 Обновление бота с GitHub

Если на GitHub появились обновления:

```bash
# Перейдите в папку проекта
cd C:\Users\alex_\OneDrive\Desktop\GruzcoBot

# Получите обновления
git pull

# Если были изменения в requirements.txt, обновите зависимости
pip install -r requirements.txt

# Перезапустите бота
python -m bot.main
```

---

## 🛑 Остановка бота

Нажмите `Ctrl + C` в консоли, где запущен бот.

Или используйте скрипт `stop_bot.ps1` (Windows PowerShell).

---

## ❌ Решение проблем

### Ошибка: "ModuleNotFoundError"

```bash
# Убедитесь, что виртуальное окружение активировано
# Переустановите зависимости
pip install -r requirements.txt
```

### Ошибка: "BOT_TOKEN not found"

- Проверьте, что файл `.env` существует в корне проекта
- Проверьте, что токен указан правильно (без пробелов)
- Убедитесь, что файл называется именно `.env` (не `.env.txt`)

### Ошибка: "Permission denied" при запуске

- Убедитесь, что используете правильную версию Python (3.11+)
- Проверьте: `python --version`

### Бот не отвечает

- Проверьте, что токен правильный
- Убедитесь, что бот запущен (нет ошибок в консоли)
- Проверьте интернет-соединение

---

## 📝 Полезные команды

```bash
# Проверить статус Git
git status

# Посмотреть логи
# Windows:
type logs\bot.log

# Linux/Mac:
cat logs/bot.log

# Деактивировать виртуальное окружение
deactivate
```

---

**Готово! Бот должен работать! 🎉**

