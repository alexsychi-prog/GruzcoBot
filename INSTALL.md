# Пошаговая инструкция по запуску GruzcoBot

## Шаг 1: Проверка Python

Убедитесь, что у вас установлен Python 3.11 или выше.

Откройте терминал (PowerShell) и выполните:
```bash
python --version
```

Если Python не установлен, скачайте его с [python.org](https://www.python.org/downloads/)

---

## Шаг 2: Создание виртуального окружения

В корневой директории проекта выполните:

```bash
python -m venv venv
```

Это создаст папку `venv` с изолированным окружением Python.

---

## Шаг 3: Активация виртуального окружения

### Для Windows (PowerShell):
```bash
venv\Scripts\Activate.ps1
```

### Для Windows (CMD):
```bash
venv\Scripts\activate.bat
```

После активации в начале строки терминала появится `(venv)`.

---

## Шаг 4: Установка зависимостей

Убедитесь, что виртуальное окружение активировано, затем выполните:

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

Это установит все необходимые библиотеки:
- aiogram 3.4.1
- aiosqlite
- SQLAlchemy
- APScheduler
- python-dotenv
- pydantic

---

## Шаг 5: Проверка файла .env

Убедитесь, что файл `.env` существует в корне проекта и содержит:

```
BOT_TOKEN=8054641558:AAHsMnTYPcCL7DJdWkjlMutnXWD4FI6PXS8
ADMIN_TELEGRAM_ID=985309476
DATABASE_URL=sqlite+aiosqlite:///data/bot.db
LOG_LEVEL=INFO
```

Если файла нет, создайте его вручную.

---

## Шаг 6: Запуск бота

Выполните команду:

```bash
python -m bot.main
```

Или альтернативный вариант:

```bash
python bot/main.py
```

---

## Шаг 7: Проверка работы

После запуска вы должны увидеть в консоли:
```
INFO - Database initialized
INFO - Scheduler started
INFO - Bot starting...
```

Бот готов к работе! Откройте Telegram и найдите вашего бота, отправьте команду `/start`.

---

## Остановка бота

Для остановки нажмите `Ctrl + C` в терминале.

---

## Возможные проблемы и решения

### Проблема: "ModuleNotFoundError"
**Решение:** Убедитесь, что виртуальное окружение активировано и зависимости установлены.

### Проблема: "Permission denied" при активации venv
**Решение (PowerShell):**
```bash
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### Проблема: "BOT_TOKEN not found"
**Решение:** Проверьте наличие файла `.env` в корне проекта с правильными значениями.

### Проблема: Ошибки с базой данных
**Решение:** Убедитесь, что папка `data/` существует. Бот создаст её автоматически при первом запуске.

---

## Запуск в фоновом режиме (Windows)

Для запуска бота в фоновом режиме используйте:

```bash
Start-Process python -ArgumentList "-m bot.main" -WindowStyle Hidden
```

Или создайте `.bat` файл:
```batch
@echo off
cd /d %~dp0
venv\Scripts\python.exe -m bot.main
pause
```

---

## Логи

Логи сохраняются в:
- `logs/bot.log` - файл логов
- Консоль - для отладки

---

## Проверка статуса

После запуска бота:
1. Откройте Telegram
2. Найдите вашего бота по username
3. Отправьте команду `/start`
4. Если вы администратор (ID: 985309476), увидите меню администратора
5. Если вы менеджер, увидите меню менеджера

---

## Готово! 🎉

Бот запущен и готов к работе!

