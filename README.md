# GruzcoBot - Telegram Bot для управления задачами

Production-ready Telegram-бот для автоматизации управления задачами сотрудников.

## 🚀 Технологический стек

- Python 3.11+
- aiogram 3.x
- SQLite (с SQLAlchemy)
- APScheduler
- FSM для диалогов
- Строгая валидация данных
- Логирование

## 📋 Функционал

### Для менеджеров:
- 📋 Просмотр активных задач
- ✅ Отметка выполнения задач
- ❌ Отметка невыполнения с указанием причины и нового дедлайна
- ⏰ Автоматические напоминания в день дедлайна

### Для администратора:
- ➕ Добавление задач менеджерам
- 📊 Просмотр всех задач
- 📈 Анализ Telegram-групп
- 🏆 Рейтинг менеджеров
- 🗑️ Очистка выполненных задач (старше 7 дней)

## 🛠️ Установка и запуск

### 1. Клонирование проекта с GitHub

```bash
# Клонируйте репозиторий
git clone https://github.com/alexsychi-prog/GruzcoBot.git

# Перейдите в папку проекта
cd GruzcoBot
```

### 2. Создание виртуального окружения

```bash
# Создайте виртуальное окружение
python -m venv venv

# Активируйте виртуальное окружение
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### 3. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
# Telegram Bot Configuration
BOT_TOKEN=ваш_токен_от_BotFather
ADMIN_TELEGRAM_ID=ваш_telegram_id

# Database URL (по умолчанию SQLite)
DATABASE_URL=sqlite+aiosqlite:///data/bot.db

# Log Level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
```

**Как получить токен:**
1. Найдите [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot` и следуйте инструкциям
3. Скопируйте полученный токен

**Как получить Telegram ID:**
1. Найдите [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте `/start`
3. Скопируйте ваш ID

**Важно:** Файл `.env` не должен попадать в Git-репозиторий!

### 4. Запуск бота

```bash
# Через Python модуль
python -m bot.main

# Или через скрипт (Windows)
start_bot.bat
```

После запуска вы увидите сообщение: "Бот запущен и готов к работе!"

Откройте Telegram, найдите вашего бота и отправьте `/start`.

## 📁 Структура проекта

```
GruzcoBot/
├── bot/
│   ├── main.py                 # Точка входа
│   ├── config.py               # Конфигурация
│   ├── database/               # Модели и работа с БД
│   ├── handlers/               # Обработчики сообщений
│   ├── keyboards/              # Клавиатуры
│   ├── services/               # Бизнес-логика
│   ├── middlewares/            # Middleware
│   └── states/                 # FSM состояния
├── data/                       # База данных
├── logs/                       # Логи
├── exports/                    # Экспортированные файлы
└── requirements.txt
```

## 🔧 Настройка анализа Telegram-групп

Для работы анализа групп:

1. Добавьте бота в группу как администратора
2. Бот автоматически начнёт собирать аналитику:
   - Общее количество участников
   - Количество вышедших
   - Количество исключённых
   - Username вышедших участников

**Важно:** Для получения полного списка участников группы может потребоваться использование Telegram Client API (Telethon/Pyrogram). Текущая реализация отслеживает события входа/выхода участников.

## 📝 Использование

### Команды

- `/start` - Запуск бота и отображение главного меню

### Для менеджеров:
- Нажмите "📋 Мои задачи" для просмотра активных задач
- Выберите задачу и отметьте выполнение или невыполнение

### Для администратора:
- Используйте меню для управления задачами и просмотра аналитики

## 🔐 Роли

- **ADMIN** - определяется по Telegram ID в `.env`
- **MANAGER** - все остальные пользователи

## ⚙️ Автоматизация

- **Напоминания о дедлайнах:** каждый день в 9:00
- **Автоочистка задач:** каждые 7 дней в 3:00

## 📊 База данных

Используется SQLite по умолчанию. Для PostgreSQL измените `DATABASE_URL` в `.env`:

```env
DATABASE_URL=postgresql+asyncpg://user:password@localhost/dbname
```

## 🐛 Логирование

Логи сохраняются в:
- `logs/bot.log` - файл логов
- Консоль - для отладки

## 📄 Лицензия

MIT

