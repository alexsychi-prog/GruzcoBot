# 🚀 Пошаговая инструкция по запуску бота

## Шаг 1: Открыть терминал PowerShell

1. Нажмите `Win + X` или кликните правой кнопкой на меню "Пуск"
2. Выберите **"Windows PowerShell"** или **"Терминал"**

## Шаг 2: Перейти в папку проекта

Введите команду (замените путь на ваш, если отличается):

```powershell
cd C:\Users\alex_\OneDrive\Desktop\GruzcoBot
```

Нажмите `Enter`.

## Шаг 3: Проверить наличие Python

Введите:

```powershell
python --version
```

Должна отобразиться версия Python (например, `Python 3.14.x`). Если ошибка, установите Python 3.11+.

## Шаг 4: Активировать виртуальное окружение (если создано)

Если виртуальное окружение уже создано, активируйте его:

```powershell
.\venv\Scripts\Activate.ps1
```

Если появится ошибка о политике выполнения, выполните:

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

Затем снова попробуйте активировать окружение.

**Если виртуальное окружение НЕ создано**, создайте его:

```powershell
python -m venv venv
.\venv\Scripts\Activate.ps1
```

## Шаг 5: Установить зависимости

Установите все необходимые пакеты:

```powershell
pip install -r requirements.txt
```

Если возникнут ошибки, попробуйте:

```powershell
pip install aiogram aiosqlite SQLAlchemy APScheduler python-dotenv pydantic-settings tzlocal
```

## Шаг 6: Запустить бота

Выполните команду:

```powershell
python -m bot.main
```

## Шаг 7: Проверить работу

После запуска вы должны увидеть в терминале:

```
2026-01-13 ... - Database initialized
2026-01-13 ... - Scheduler started
2026-01-13 ... - Bot starting...
2026-01-13 ... - Start polling
```

**Это означает, что бот успешно запущен!** ✅

## Шаг 8: Проверить в Telegram

1. Откройте Telegram
2. Найдите вашего бота по имени или username
3. Отправьте команду `/start`
4. Если вы админ (ID: 985309476), увидите меню с 5 кнопками
5. Если вы менеджер, увидите кнопку "📋 Мои задачи"

---

## ⚠️ Решение проблем

### Проблема: "ModuleNotFoundError"

**Решение:** Установите недостающий модуль:
```powershell
pip install <имя_модуля>
```

### Проблема: "BOT_TOKEN не найден"

**Решение:** Проверьте файл `.env` в корне проекта. Он должен содержать:
```
BOT_TOKEN=8054641558:AAHsMnTYPcCL7DJdWkjlMutnXWD4FI6PXS8
ADMIN_TELEGRAM_ID=985309476
```

### Проблема: "ExecutionPolicy"

**Решение:** Выполните:
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### Проблема: "Conflict: terminated by other getUpdates request"

**Причина:** Запущено несколько экземпляров бота одновременно.

**Решение:**
1. Остановите все запущенные процессы Python:
   ```powershell
   Get-Process python | Stop-Process -Force
   ```
   
   Или используйте скрипт:
   ```powershell
   powershell -ExecutionPolicy Bypass -File stop_bot.ps1
   ```

2. Подождите 3-5 секунд

3. Запустите бота заново:
   ```powershell
   python -m bot.main
   ```

### Проблема: Бот не отвечает в Telegram

**Проверьте:**
1. Бот запущен (в терминале нет ошибок)
2. Токен бота правильный
3. Интернет-соединение работает
4. Бот не заблокирован в Telegram

---

## 🛑 Остановка бота

Чтобы остановить бота, нажмите в терминале:
- `Ctrl + C`

---

## 📝 Быстрый запуск (если всё уже установлено)

Если все зависимости уже установлены, просто выполните:

```powershell
cd C:\Users\alex_\OneDrive\Desktop\GruzcoBot
python -m bot.main
```

---

## 🔄 Автозапуск при перезагрузке (опционально)

Чтобы бот запускался автоматически, можно создать `.bat` файл:

1. Создайте файл `start_bot.bat` в папке проекта
2. Добавьте в него:
```batch
@echo off
cd /d "C:\Users\alex_\OneDrive\Desktop\GruzcoBot"
python -m bot.main
pause
```

3. Дважды кликните на файл для запуска

---

## 📞 Поддержка

Если проблемы остались, проверьте:
- Логи в папке `logs/bot.log`
- Файл `.env` существует и содержит правильные данные
- Python версии 3.11 или выше установлен

