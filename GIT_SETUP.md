# Инструкция по настройке Git и загрузке на GitHub

## ✅ Что уже сделано:

1. ✅ Обновлен `.gitignore` - исключены все ненужные файлы
2. ✅ Удалены реальные токены из `bot/config.py`
3. ✅ Создан `.env.example` - шаблон для переменных окружения
4. ✅ Обновлен `README.md` с инструкциями

## 📋 Команды для настройки Git и загрузки на GitHub

### 1. Установите Git (если еще не установлен)

Скачайте с официального сайта: https://git-scm.com/download/win

### 2. Инициализация Git-репозитория

```bash
# Перейдите в папку проекта
cd C:\Users\alex_\OneDrive\Desktop\GruzcoBot

# Инициализируйте Git
git init

# Настройте имя и email (если еще не настроено)
git config --global user.name "Ваше Имя"
git config --global user.email "your.email@example.com"
```

### 3. Добавьте все файлы в индекс

```bash
# Проверьте, какие файлы будут добавлены
git status

# Добавьте все файлы (кроме тех, что в .gitignore)
git add .
```

### 4. Создайте первый коммит

```bash
git commit -m "Initial commit"
```

### 5. Создайте репозиторий на GitHub

1. Зайдите на https://github.com
2. Нажмите "New repository" (или "+" → "New repository")
3. Укажите имя репозитория (например: `GruzcoBot`)
4. **НЕ** добавляйте README, .gitignore или лицензию (они уже есть)
5. Нажмите "Create repository"

### 6. Подключите локальный репозиторий к GitHub

```bash
# Добавьте remote (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/GruzcoBot.git

# Или если используете SSH:
# git remote add origin git@github.com:YOUR_USERNAME/GruzcoBot.git
```

### 7. Загрузите код на GitHub

```bash
# Переименуйте ветку в main (если нужно)
git branch -M main

# Загрузите код
git push -u origin main
```

## 🔐 Важно: Безопасность

- ✅ Файл `.env` уже в `.gitignore` - он не попадет в репозиторий
- ✅ Реальные токены удалены из кода
- ✅ Создан `.env.example` для примера

**Перед загрузкой убедитесь, что:**
- Нет файла `.env` в репозитории (проверьте: `git status`)
- В коде нет реальных токенов или паролей

## 📝 Проверка перед коммитом

```bash
# Проверьте, что .env не попадет в коммит
git status

# Должно быть видно, что .env игнорируется
```

## 🚀 После загрузки

Заказчик сможет:
1. Клонировать репозиторий: `git clone https://github.com/YOUR_USERNAME/GruzcoBot.git`
2. Создать `.env` на основе `.env.example`
3. Установить зависимости: `pip install -r requirements.txt`
4. Запустить бота: `python -m bot.main`

